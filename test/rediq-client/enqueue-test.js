const should = require('should')
const {Client} = require('rediq')

describe('rediq-client', () => {
    describe('#enqueue', () => {
        it.skip('TODO', (done) => {
            done()
        })
    })
})
