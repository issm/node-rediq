const should = require('should')
const {Watcher, Worker} = require('rediq')

describe('rediq-watcher', () => {
    describe('#watch', () => {
        it.skip('TODO', (done) => {
            done()
        })
    })
})
